<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of Cars</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* Optional custom CSS for additional styling */
        /* Adjustments to table borders and spacing */
        table {
            border-collapse: collapse;
            width: 100%;
        }
        th, td {
            padding: 8px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
<div class="container mt-5">
    <h1 class="display-4">List of Cars</h1>

    <table class="table table-striped mt-4">
        <thead>
        <tr>
            <th>Serie</th>
            <th>Date M C</th>
            <th>Model</th>
            <th>Price per Day</th>
        </tr>
        </thead>
        <tbody>
        {% for v in listeVoiture %}
            <tr>
                <td>{{ v.id }}</td>
                <td>{{ v.dateMiseEnMarche | date }}</td>
                <td>{{ v.modele }}</td>
                <td>{{ v.prixJour }}</td>
                <td><a href="{{ path('voitureDelete', {'id': v.id}) }}">Supprimer</a> </td>
                <td><a href="{{ path('voitureUpdate', {'id': v.id}) }}">MàJ</a> </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('addvoiture') }}">Ajout Voiture</a>
</div>

<!-- Bootstrap JS (Optional, for certain Bootstrap components that require JS) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>