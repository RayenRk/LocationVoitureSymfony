<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recherche Voiture</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* Optional custom CSS for additional styling */
        /* Adjustments to form layout */
        form {
            margin-top: 20px;
        }
        /* Table styling */
        table {
            border-collapse: collapse;
            width: 100%;
            margin-top: 20px;
        }
        th, td {
            padding: 8px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
<div class="container mt-5">
    <h1 class="display-4">Recherche Voiture</h1>

    <form action="" method="post" class="mt-4">
        <div class="mb-3">
            <label for="input_serie" class="form-label">Série:</label>
            <input type="text" name="input_serie" class="form-control" id="input_serie">
        </div>
        <button type="submit" class="btn btn-primary">Recherche</button>
    </form>

    {% if voitures is empty %}
        <p class="mt-4">Aucune Voiture Trouvée</p>
    {% else %}
        <table class="table table-bordered mt-4">
            <thead>
            <tr>
                <th>Serie</th>
                <th>Modele</th>
            </tr>
            </thead>
            <tbody>
            {% for v in voitures %}
                <tr>
                    <td>{{ v.serie }}</td>
                    <td>{{ v.modele }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% endif %}
</div>

<!-- Bootstrap JS (Optional, for certain Bootstrap components that require JS) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>